// import { Page } from '../types'
// import { Entry } from './hooks'

// const toJson = (map: any) => JSON.parse(JSON.stringify(map))

export const preSavePage = {
  name: 'preSave',
  // @ts-ignore
  handler: () => { }
}
